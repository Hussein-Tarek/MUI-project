import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#0563BB"
        },
        secondary: {
            main: "#45505b"
        },
        white: {
            main: "#ffffff"
        },
    }
})
export default theme